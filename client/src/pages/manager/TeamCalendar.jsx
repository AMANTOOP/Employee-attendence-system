import { Typography } from "@mui/material";

const TeamCalendar = () => {
  return (
    <div style={{ padding: 20 }}>
      <Typography variant="h4">Team Calendar View</Typography>
      <p>(Calendar UI integration can be added using react-big-calendar or FullCalendar)</p>
    </div>
  );
};

export default TeamCalendar;
